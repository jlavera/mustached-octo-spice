<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <configSections>
  </configSections>
  <connectionStrings>
    <!-- Tal como indica la documentación desde antes de la primer entrega, el string de conexión está en el archivo Clases/DB.cs -->
  </connectionStrings>
</configuration>